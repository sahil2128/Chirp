/*
Provide general utility that makes stuff slightly easier
*/
#pragma once

#include <string>

namespace utils
{
    /*
    Returns true if the given string is only composed of digits
    */
    bool is_number(std::string);
}